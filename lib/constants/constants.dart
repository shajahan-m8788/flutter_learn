import 'package:flutter/material.dart';

class AppConstants {
  static const Radius defaultBorderRadius = Radius.circular(8);
  // static const
  static const Widget textFieldGap = SizedBox(
    height: 16,
  );
}
